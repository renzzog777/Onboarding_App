# Copy this file to .env on the VPS and fill in a real password.
# .env is never committed to git (see .gitignore) - it only ever lives
# on the VPS itself.

ADMIN_PASSWORD=change-this-to-a-real-password
